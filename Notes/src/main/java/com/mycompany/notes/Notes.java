

Notes for Java calss
    /*
    
    
    class => blueprint of the object.
        A class is a structure that defines how an object is constructed.
        this structure cntains both data and methods supported by that particular object.

                For eg:
               class Student{
               private String name;
               private int age;
               private String address;
               private int rollNo;

                public String findName(int rollNo);
               public void editName(String name);
               public void displayDetail();
               public void payFee();
               }
        When a class in instantiated to be used in the progeam is it called an object. Obect consumes the memory but class is just a mere representation of the object structure and hnece no memroy is used at all.

        Complex Number => realpart + imaginarypart

                4+7i
                5+9i
                ----
                9+16i

                i*i = -1
    
    
    
    
    
        Functions:
    
    
            1. Function with no return value and no argument;
                    void fxnName();

            2. Function with return value and no argument;
                    dataType fxnName(); // dataType can be any class, wrapper class, basic data type like, int, String, float, double, boolean

                    int fxnName();
                   String fxnName();
            3. Function with no return value but arguments;
                    void fxnName(dataType x, dataType y, dataType z);

                    void printName(String fName, String lName);

            4. Function with return value and also arguments;
                    dataType fxnName(dataType x, dataType y, dataType z);

                    int addNumber(int a, int b);

                    double divNum(int num, int deno);






        inheritage :
        we should see that it
        is a relation    ,...we can use inheritage in this case
        has a relation   

        
        
        
        sc.nextLine();   it is used while int variable is inserted







seperatio of consion

model class or inteti (account)
        view (bankmain app)
        controller (bankcontroller)


java collection

     */

    